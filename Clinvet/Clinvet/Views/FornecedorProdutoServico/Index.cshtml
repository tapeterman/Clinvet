@model IEnumerable<Clinvet.Models.FornecedorProdutoServico>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.IdFornecedor)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IdProduto)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.IdFornecedor)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IdProduto)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdFornecedorProduto }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdFornecedorProduto }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdFornecedorProduto })
        </td>
    </tr>
}

</table>
