@model IEnumerable<Clinvet.Models.Anamnese>

@{
    ViewBag.Title = "Index";
}
<section class="content profile-page">
    <div class="block-header">
        <div class="row">
            <div class="col-lg-7 col-md-6 col-sm-12">
                <h2>
                    Anamnese
                </h2>
            </div>
            <div class="col-lg-5 col-md-6 col-sm-12">
                <button class="btn btn-white btn-icon btn-round hidden-sm-down float-right m-l-10" type="button">
                    <i class="zmdi zmdi-plus"></i>
                </button>
                <ul class="breadcrumb float-md-right">
                    <li class="breadcrumb-item"><a href="@Url.Action("Index", "Home")"><i class="zmdi zmdi-home"></i> SysComPet</a></li>
                    <li class="breadcrumb-item active">Anamnese</li>
                </ul>
            </div>
        </div>
    </div>
    <div class="container-fluid">
        <div class="row clearfix">
            <div class="col-md-12">
                <div class="card parents-list">
                    <div class="header">
                        <h2> Lista de Anamnese Cadastrados</h2>
                        <ul class="header-dropdown">
                            <li class="remove">
                                <a href="@Url.Action("Create", "Anamnese")" class="btn btn-raised btn-primary btn-default btn-round waves-effect">Cadastrar Nova Anamnese</a>
                            </li>
                        </ul>
                    </div>
                    <div class="body table-responsive">
                        <table class="table table-bordered table-striped table-hover js-basic-example dataTable">
                            <thead>
                                <tr>
                                    <th>
                                        @Html.DisplayNameFor(model => model.FichaAnimal.Animal.Nome)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Diagnostico)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Anamnese1)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.Medicamentos)
                                    </th>
                                    <th>
                                        @Html.DisplayNameFor(model => model.ProcedimentosRealizados)
                                    </th>
                                    <th> Ações</th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model) {
                                    <tr>
                                        <td><strong class="col-purple">@Html.DisplayFor(modelItem => item.FichaAnimal.Animal.Nome)</strong></td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Diagnostico)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Anamnese1)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.Medicamentos)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => item.ProcedimentosRealizados)
                                        </td>
                                        <td>
                                            @Html.ActionLink("Editar", "Edit", new { id = item.Id }, new { @class = "btn btn-primary btn-sm" })
                                            @Html.ActionLink("Detalhes", "Details", new { id = item.Id }, new { @class = "btn btn-primary btn-sm" })
                                            @Html.ActionLink("Apagar", "Delete", new { id = item.Id }, new { @class = "btn btn-primary btn-sm" })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>