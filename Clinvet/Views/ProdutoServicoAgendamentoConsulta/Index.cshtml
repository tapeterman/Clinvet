@model IEnumerable<Clinvet.Models.ProdutoServicoAgendamentoConsultas>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Agendamentoconsulta.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ProdutoServico.Nome)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Agendamentoconsulta.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ProdutoServico.Nome)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdProdutoServicoAgendamentoConsulta }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdProdutoServicoAgendamentoConsulta }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdProdutoServicoAgendamentoConsulta })
        </td>
    </tr>
}

</table>
