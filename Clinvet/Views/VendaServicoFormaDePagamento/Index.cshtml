@model IEnumerable<Clinvet.Models.VendaServicoFormaDePagamento>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FormaDePagamento.Descricao)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.VendaServico.Descricao)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FormaDePagamento.Descricao)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.VendaServico.Descricao)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.IdVendaServicoFormaDePagamento }) |
            @Html.ActionLink("Details", "Details", new { id=item.IdVendaServicoFormaDePagamento }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.IdVendaServicoFormaDePagamento })
        </td>
    </tr>
}

</table>
